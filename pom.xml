<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.trade</groupId>
    <artifactId>stock-trading</artifactId>
    <version>1.0.0</version>
    <packaging>war</packaging>

    <name>Stock Trading System</name>
    <description>주식 매매 시스템 - REST API 및 Spring Batch 기반 대용량 처리</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>1.8</java.version>

        <!-- Spring Ecosystem -->
        <spring.version>4.2.7.RELEASE</spring.version>
        <spring-batch.version>3.0.10.RELEASE</spring-batch.version>
        <spring-data-jpa.version>1.10.6.RELEASE</spring-data-jpa.version>
        <spring-data-redis.version>1.7.11.RELEASE</spring-data-redis.version>

        <!--
            Hibernate 4.3.x 선택 이유:
            Spring 4.2.x 공식 호환 범위이며, JPA 2.1 스펙을 완전 지원한다.
            5.0.x도 가능하지만 4.3이 이 Spring 버전대에서 가장 검증된 조합.
        -->
        <hibernate.version>4.3.11.Final</hibernate.version>
        <hibernate-validator.version>5.2.5.Final</hibernate-validator.version>

        <!-- MyBatis -->
        <mybatis.version>3.5.6</mybatis.version>
        <mybatis-spring.version>1.3.3</mybatis-spring.version>

        <!-- Connection Pool & DB Driver -->
        <hikaricp.version>3.4.5</hikaricp.version>
        <mariadb.version>2.7.6</mariadb.version>
        <h2.version>1.4.200</h2.version>

        <!-- Redis -->
        <jedis.version>2.9.3</jedis.version>

        <!-- Web & Serialization -->
        <jackson.version>2.6.7</jackson.version>
        <servlet.version>3.1.0</servlet.version>

        <!-- Logging -->
        <logback.version>1.2.12</logback.version>
        <slf4j.version>1.7.36</slf4j.version>

        <!-- Test -->
        <junit-jupiter.version>5.9.3</junit-jupiter.version>
    </properties>

    <dependencies>

        <!-- ====================== Spring Core ====================== -->
        <!--
            commons-logging 제거 필수:
            Spring이 내부적으로 JCL(Jakarta Commons Logging)에 의존하는데,
            Logback을 쓰려면 JCL -> SLF4J 브릿지(jcl-over-slf4j)로 우회해야 한다.
            안 빼면 클래스패스 충돌로 로그가 이상하게 찍히는 지옥을 맛본다.
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-orm</artifactId>
            <version>${spring.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!-- ====================== Spring Batch ====================== -->
        <dependency>
            <groupId>org.springframework.batch</groupId>
            <artifactId>spring-batch-core</artifactId>
            <version>${spring-batch.version}</version>
        </dependency>

        <!-- ====================== Spring Data ====================== -->
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-jpa</artifactId>
            <version>${spring-data-jpa.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-redis</artifactId>
            <version>${spring-data-redis.version}</version>
        </dependency>

        <!-- ====================== JPA / Hibernate ====================== -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-entitymanager</artifactId>
            <version>${hibernate.version}</version>
        </dependency>

        <!-- ====================== MyBatis ====================== -->
        <dependency>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis</artifactId>
            <version>${mybatis.version}</version>
        </dependency>
        <dependency>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis-spring</artifactId>
            <version>${mybatis-spring.version}</version>
        </dependency>

        <!-- ====================== Database ====================== -->
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
            <version>${mariadb.version}</version>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
            <scope>test</scope>
        </dependency>
        <!--
            HikariCP 선택 이유:
            DBCP2 대비 커넥션 획득 속도가 월등하고, 금융 트랜잭션처럼
            짧고 빈번한 DB 접근 패턴에서 성능 차이가 유의미하다.
            Java 8 환경에서는 3.x 라인이 마지막 지원 버전.
        -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>${hikaricp.version}</version>
        </dependency>

        <!-- ====================== Redis ====================== -->
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
            <version>${jedis.version}</version>
        </dependency>

        <!-- ====================== Web / JSON ====================== -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>${servlet.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- ====================== Validation ====================== -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-validator</artifactId>
            <version>${hibernate-validator.version}</version>
        </dependency>

        <!-- ====================== Logging ====================== -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
        <!-- Spring 내부의 commons-logging 호출을 SLF4J로 리다이렉트 -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!-- ====================== Test ====================== -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.2</version>
                <configuration>
                    <!-- Java Config 기반이므로 web.xml 없이 WAR 패키징 -->
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <!-- JUnit 5 구동을 위해 Surefire 2.22+ 필수 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M9</version>
            </plugin>
        </plugins>
    </build>

</project>
